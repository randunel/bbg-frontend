{
  "name": "bbg-frontend",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -r docs && cp -r dist docs && sed -i 's/=\"\\//=\".\\//' docs/index.html && touch docs/.nojekyll"
  },
  "dependencies": {
    "vue": "^3.0.0-rc.1",
    "@automationcloud/cloud-robot": "^1.0.0-rc.10"
  },
  "devDependencies": {
    "vite": "^1.0.0-rc.1",
    "@vue/compiler-sfc": "^3.0.0-rc.1"
  }
}
